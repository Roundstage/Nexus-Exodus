{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher",
  "description": "Only the packaged launcher receives core window permissions.",
  "windows": ["launcher"],
  "permissions": ["core:default"]
}
